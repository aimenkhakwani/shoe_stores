<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <link href="/css/bootstrap.min.css" rel="stylesheet" type="text/css">
    <link href="https://fonts.googleapis.com/css?family=Open+Sans|Prosto+One" rel="stylesheet">
    <link href="/css/styles.css" rel="stylesheet" type="text/css">
    <title>Local Shoe Stores</title>
</head>
<body>
    <div class="container">
        <h1><a href="/">Local Shoe Stores Database</a></h1>
        <ul class="nav nav-tabs">
            <li role="presentation"><a href="/">Home</a></li>
            <li role="presentation"><a href="/allStores">Stores</a></li>
            <li role="presentation"><a href="/allBrands">Brands</a></li>
        </ul>
        <h2>{{store.getName}}</h2>
        <h4>Address: {{store.getAddress}}</h4>
        <p><a href="/updateOrDelete/{{store.getId}}">Update or Delete {{store.getName}}</a></p>
        <br>
        <div class="row">
            <div class="col-md-6">
                <h3>Add a brand to {{store.getName}}</h3>
                <form action="/addBrandToStore" method="post">
                    <input name="store_id" type="hidden" value="{{ store.getId }}">
                    <select class="form-control" name="brand_id" type="text">
                        {% for brand in all_brands %}
                            <option value="{{ brand.getId }}">{{ brand.getName }}</li>
                        {% endfor %}
                    </select>
                    <br>
                    <button class="btn btn-defualt center-block" type="submit">Add Brand</button>
                </form>
            </div>
            <div class="col-md-2"></div>
            <div class="col-md-4">
                <h3>All brands that {{store.getName}} carries:</h3>
                {% if brands is not empty %}
                <ul>
                    {% for brand in brands %}
                        <li><a href="/thisBrand/{{brand.getId}}">{{ brand.getName }}</a></li>
                    {% endfor %}
                </ul>
                {% endif %}
            </div>
        </div>
    </div>
</body>
</html>
