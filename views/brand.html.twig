<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <link href="/css/bootstrap.min.css" rel="stylesheet" type="text/css">
    <link href="https://fonts.googleapis.com/css?family=Open+Sans|Prosto+One" rel="stylesheet">
    <link href="/css/styles.css" rel="stylesheet" type="text/css">
    <title>Local Shoe Stores</title>
</head>
<body>
    <div class="container">
        <h1><a href="/">Local Shoe Stores Database</a></h1>
        <ul class="nav nav-tabs">
            <li role="presentation"><a href="/">Home</a></li>
            <li role="presentation"><a href="/allStores">Stores</a></li>
            <li role="presentation"><a href="/allBrands">Brands</a></li>
        </ul>
        <h2>{{brand.getName}}</h2>
        <br>
        <div class="row">
            <div class="col-md-6">
                <h3>Add a store to sell {{brand.getName}} shoes at:</h3>
                <form action="/addStoreToBrand" method="post">
                    <input name="brand_id" type="hidden" value="{{ brand.getId }}">
                    <select class="form-control" name="store_id" type="text">
                        {% for store in all_stores %}
                            <option value="{{ store.getId }}">{{ store.getName }}</li>
                        {% endfor %}
                    </select>
                    <br>
                    <button class="btn btn-defualt center-block" type="submit">Add Store</button>
                </form>
            </div>
            <div class="col-md-2"></div>
            <div class="col-md-4">
                <h3>All stores that {{brand.getName}} shoes are sold at:</h3>
                {% if stores is not empty %}
                <ul>
                    {% for store in stores %}
                        <li><a href="/thisStore/{{store.getId}}">{{ store.getName }}</a></li>
                    {% endfor %}
                </ul>
                {% endif %}
            </div>
        </div>
    </div>
</body>
</html>
